// Generated by tools/prototype/generate.mjs — do not edit by hand.
import React from 'react';
import { View, Text, Pressable, ScrollView, Image, StyleSheet } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { IMG } from '@/proto/theme/images';
import { AppHeader } from '@/proto/components/AppHeader';
import { TabBar } from '@/proto/components/TabBar';
import { useVals } from '@/proto/runtime/vals';

export default function ScreenA22() {
  const v = useVals();
  const insets = useSafeAreaInsets();
  return (
    <View style={s.s0}>
      <View style={[s.s1, { height: insets.top }]} />
      <AppHeader title="Messages" showBack={false} rightPlus={true} onRight={v.push_A22_M1} />
      <ScrollView style={s.s2} contentContainerStyle={s.s3} showsVerticalScrollIndicator={false}>
        {v.threads.map((t, tI) => (
            <React.Fragment key={tI}>
            <Pressable onPress={t.open} style={s.s4}>
              {t.i0 ? (
                <Image source={IMG.allocator} resizeMode="cover" accessibilityLabel="Kate Ryan" style={s.s5} />
              ) : null}
              {t.i1 ? (
                <Image source={IMG.primeMover} resizeMode="cover" accessibilityLabel="Redgum workshop" style={s.s5} />
              ) : null}
              {t.i2 ? (
                <Image source={IMG.palletsDock} resizeMode="cover" accessibilityLabel="Marco Ferretti" style={s.s5} />
              ) : null}
              <View style={s.s2}>
                <View style={s.s6}>
                  <Text style={s.s7}>
                    {t.who}
                  </Text>
                </View>
                <Text style={s.s9} numberOfLines={1}>
                  {t.preview}
                </Text>
              </View>
              <View style={s.s10}>
                <Text style={s.s11}>{t.at}</Text>
                {t.unread ? (
                  <View style={s.s8}>
                    <Text style={s.s8Text}>1</Text>
                  </View>
                ) : null}
              </View>
            </Pressable>
            </React.Fragment>
        ))}
      </ScrollView>
      <TabBar active="messages" onRun={v.tabRun} onNav={v.tab_A6} onComp={v.tab_A25} onMsg={v.tab_A22} onProf={v.tab_A34} />
    </View>
  );
}

const s = StyleSheet.create({
  s0: {
    backgroundColor: "#F4F6FA",
    flex: 1,
    flexDirection: "column",
  },
  s1: {
    backgroundColor: "#FFFFFF",
    flexGrow: 0,
    flexShrink: 0,
  },
  s2: {
    flex: 1,
  },
  s3: {
    flexGrow: 1,
    paddingBottom: 20,
    paddingLeft: 20,
    paddingRight: 20,
    paddingTop: 8,
  },
  s4: {
    alignItems: "center",
    backgroundColor: "transparent",
    borderBottomColor: "#E2E8F0",
    borderBottomWidth: 1,
    borderWidth: 0,
    flexDirection: "row",
    gap: 12,
    minHeight: 76,
    paddingBottom: 14,
    paddingLeft: 0,
    paddingRight: 0,
    paddingTop: 14,
    width: "100%",
  },
  s5: {
    borderRadius: 999,
    flexGrow: 0,
    flexShrink: 0,
    height: 48,
    width: 48,
  },
  s6: {
    alignItems: "center",
    flexDirection: "row",
    gap: 8,
  },
  s7: {
    color: "#0F1B2A",
    fontFamily: "Inter_600SemiBold",
    fontSize: 15,
    lineHeight: 20,
    textAlign: "left",
  },
  s8: {
    alignItems: "center",
    backgroundColor: "#2563EB",
    borderRadius: 999,
    height: 18,
    justifyContent: "center",
    minWidth: 18,
    paddingLeft: 5,
    paddingRight: 5,
  },
  s8Text: {
    color: "#FFFFFF",
    fontFamily: "Inter_700Bold",
    fontSize: 10,
    lineHeight: 12,
    textAlign: "center",
  },
  s9: {
    color: "#5B6B7F",
    fontFamily: "Inter_400Regular",
    fontSize: 13,
    lineHeight: 18,
    textAlign: "left",
  },
  s10: {
    alignItems: "flex-end",
    flexGrow: 0,
    flexShrink: 0,
    gap: 6,
  },
  s11: {
    color: "#5B6B7F",
    fontFamily: "RobotoMono_400Regular",
    fontSize: 12,
    lineHeight: 16,
    textAlign: "left",
  },
});
