// Generated by tools/prototype/generate.mjs — do not edit by hand.
import { useState } from 'react';
import { View, Text, Pressable, ScrollView, TextInput, StyleSheet } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import Svg, { Circle, Path } from 'react-native-svg';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { AppHeader } from '@/proto/components/AppHeader';
import { useVals } from '@/proto/runtime/vals';

export default function ScreenB1() {
  const v = useVals();
  const insets = useSafeAreaInsets();
  const [form, setForm] = useState({ name: '', mobile: '', email: '', licence: '', licenceClass: '', state: '', password: '', confirmPassword: '' });
  const [showPassword, setShowPassword] = useState(false);
  const [showConfirmPassword, setShowConfirmPassword] = useState(false);
  const [touched, setTouched] = useState<Record<string, boolean>>({});
  const setField = (key: keyof typeof form, value: string) => setForm((old) => ({ ...old, [key]: value }));
  const errors: Record<keyof typeof form, string> = {
    name: !form.name.trim() ? 'Full name is required' : form.name.trim().length < 2 ? 'Enter your full name' : '',
    mobile: !form.mobile.trim() ? 'Mobile number is required' : !/^\+?[\d\s()-]{8,15}$/.test(form.mobile.trim()) ? 'Enter a valid mobile number' : '',
    email: !form.email.trim() ? 'Email is required' : !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email.trim()) ? 'Enter a valid email address' : '',
    licence: !form.licence.trim() ? 'Licence number is required' : '',
    licenceClass: !form.licenceClass.trim() ? 'Class is required' : '',
    state: !form.state.trim() ? 'State of issue is required' : '',
    password: !form.password ? 'Password is required' : form.password.length < 8 ? 'Use at least 8 characters' : !/(?=.*[A-Za-z])(?=.*\d)/.test(form.password) ? 'Include a letter and a number' : '',
    confirmPassword: !form.confirmPassword ? 'Confirm your password' : form.confirmPassword !== form.password ? 'Passwords do not match' : '',
  };
  const blur = (key: keyof typeof form) => setTouched((old) => ({ ...old, [key]: true }));
  const submit = () => {
    setTouched(Object.fromEntries(Object.keys(form).map((key) => [key, true])));
    if (Object.values(errors).every((error) => !error)) {
      const firstName = form.name.trim().split(/\s+/)[0];
      v.setUserName(firstName.charAt(0).toUpperCase() + firstName.slice(1).toLowerCase());
      v.registerContinue();
    }
  };
  const field = (key: keyof typeof form, props = {}) => (
    <>
      <TextInput style={[s.s8, touched[key] && errors[key] ? s.inputError : null]} value={form[key]} onChangeText={(value) => setField(key, value)} onBlur={() => blur(key)} placeholderTextColor="#94A3B8" {...props} />
      {touched[key] && errors[key] ? <Text style={s.errorText}>{errors[key]}</Text> : null}
    </>
  );
  return (
    <View style={s.s0}>
      <View style={[s.s1, { height: insets.top }]} />
      <AppHeader title="Create an account" sub="Yours, not an operator's" showBack={true} onBack={v.back} />
      <ScrollView style={s.s2} contentContainerStyle={s.s3} showsVerticalScrollIndicator={false}>
        <View style={s.s4}>
          <Svg width={20} height={20} viewBox="0 0 24 24" fill="none" stroke="#2563EB" strokeWidth="1.8" strokeLinecap="round">
            <Circle cx="12" cy="12" r="9" />
            <Path d="M12 11v5" />
            <Path d="M12 8h.01" />
          </Svg>
          <Text style={s.s5}>
            Your account and your documents belong to you. Operators are added afterwards, one at a time, and each only sees what you allow.
          </Text>
        </View>
        <View style={s.s6}>
          <Text style={s.s7}>
            Full name
          </Text>
          {field('name', { placeholder: 'Enter your full name', textContentType: 'name', autoCapitalize: 'words' })}
        </View>
        <View style={s.s6}>
          <Text style={s.s7}>
            Mobile
          </Text>
          {field('mobile', { placeholder: 'Enter your mobile number', keyboardType: 'phone-pad', textContentType: 'telephoneNumber' })}
        </View>
        <View style={s.s6}>
          <Text style={s.s7}>
            Email
          </Text>
          {field('email', { placeholder: 'you@example.com.au', keyboardType: 'email-address', autoCapitalize: 'none', autoCorrect: false, textContentType: 'emailAddress' })}
        </View>
        <View style={s.s9}>
          <View style={s.s10}>
            <Text style={s.s7}>
              Licence number
            </Text>
            <TextInput style={[s.s11, touched.licence && errors.licence ? s.inputError : null]} value={form.licence} onChangeText={(value) => setField('licence', value)} onBlur={() => blur('licence')} placeholder="Number" placeholderTextColor="#94A3B8" autoCapitalize="characters" />
            {touched.licence && errors.licence ? <Text style={s.errorText}>{errors.licence}</Text> : null}
          </View>
          <View style={s.s12}>
            <Text style={s.s7}>
              Class
            </Text>
            <TextInput style={[s.s11, touched.licenceClass && errors.licenceClass ? s.inputError : null]} value={form.licenceClass} onChangeText={(value) => setField('licenceClass', value)} onBlur={() => blur('licenceClass')} placeholder="MC" placeholderTextColor="#94A3B8" autoCapitalize="characters" />
            {touched.licenceClass && errors.licenceClass ? <Text style={s.errorText}>{errors.licenceClass}</Text> : null}
          </View>
        </View>
        <View style={s.s6}>
          <Text style={s.s7}>
            State of issue
          </Text>
          {field('state', { placeholder: 'e.g. Victoria', autoCapitalize: 'words' })}
        </View>
        <View style={s.s6}>
          <Text style={s.s7}>Password</Text>
          <View style={[s.passwordBox, touched.password && errors.password ? s.inputError : null]}>
            <TextInput style={s.passwordInput} value={form.password} onChangeText={(value) => setField('password', value)} onBlur={() => blur('password')} placeholder="At least 8 characters" placeholderTextColor="#94A3B8" secureTextEntry={!showPassword} autoCapitalize="none" textContentType="newPassword" />
            <Pressable onPress={() => setShowPassword((old) => !old)} hitSlop={10} accessibilityRole="button" accessibilityLabel={showPassword ? 'Hide password' : 'Show password'}><Ionicons name={showPassword ? 'eye-outline' : 'eye-off-outline'} size={22} color="#5B6B7F" /></Pressable>
          </View>
          {touched.password && errors.password ? <Text style={s.errorText}>{errors.password}</Text> : null}
        </View>
        <View style={s.s6}>
          <Text style={s.s7}>Confirm password</Text>
          <View style={[s.passwordBox, touched.confirmPassword && errors.confirmPassword ? s.inputError : null]}>
            <TextInput style={s.passwordInput} value={form.confirmPassword} onChangeText={(value) => setField('confirmPassword', value)} onBlur={() => blur('confirmPassword')} placeholder="Re-enter your password" placeholderTextColor="#94A3B8" secureTextEntry={!showConfirmPassword} autoCapitalize="none" textContentType="newPassword" />
            <Pressable onPress={() => setShowConfirmPassword((old) => !old)} hitSlop={10} accessibilityRole="button" accessibilityLabel={showConfirmPassword ? 'Hide password' : 'Show password'}><Ionicons name={showConfirmPassword ? 'eye-outline' : 'eye-off-outline'} size={22} color="#5B6B7F" /></Pressable>
          </View>
          {touched.confirmPassword && errors.confirmPassword ? <Text style={s.errorText}>{errors.confirmPassword}</Text> : null}
        </View>
        <Pressable onPress={submit} style={s.s13}>
          <Text style={s.s14}>Continue to your documents</Text>
        </Pressable>
        <Text style={s.s15}>
          FleetSync checks your licence against VicRoads before any operator can allocate you work.
        </Text>
      </ScrollView>
    </View>
  );
}

const s = StyleSheet.create({
  s0: {
    backgroundColor: "#FFFFFF",
    flex: 1,
    flexDirection: "column",
  },
  s1: {
    flexGrow: 0,
    flexShrink: 0,
  },
  s2: {
    flex: 1,
  },
  s3: {
    flexDirection: "column",
    flexGrow: 1,
    gap: 14,
    paddingBottom: 20,
    paddingLeft: 20,
    paddingRight: 20,
    paddingTop: 20,
  },
  s4: {
    alignItems: "flex-start",
    backgroundColor: "#EEF3F9",
    borderRadius: 12,
    flexDirection: "row",
    gap: 10,
    paddingBottom: 12,
    paddingLeft: 14,
    paddingRight: 14,
    paddingTop: 12,
  },
  s5: {
    color: "#0F1B2A",
    fontFamily: "Inter_400Regular",
    fontSize: 13,
    lineHeight: 18,
  },
  s6: {
    flexDirection: "column",
    flexShrink: 0,
    gap: 6,
  },
  s7: {
    color: "#5B6B7F",
    fontFamily: "Inter_600SemiBold",
    fontSize: 11,
    letterSpacing: 0.88,
    lineHeight: 14,
    textTransform: "uppercase",
  },
  s8: {
    backgroundColor: "#FFFFFF",
    borderColor: "#E2E8F0",
    borderRadius: 10,
    borderWidth: 1,
    color: "#0F1B2A",
    flexShrink: 0,
    fontFamily: "Inter_400Regular",
    fontSize: 16,
    height: 52,
    paddingBottom: 0,
    paddingLeft: 14,
    paddingRight: 14,
    paddingTop: 0,
    width: "100%",
  },
  s9: {
    flexDirection: "row",
    gap: 10,
  },
  s10: {
    flex: 1,
    flexDirection: "column",
    flexShrink: 0,
    gap: 6,
  },
  s11: {
    backgroundColor: "#FFFFFF",
    borderColor: "#E2E8F0",
    borderRadius: 10,
    borderWidth: 1,
    color: "#0F1B2A",
    flexShrink: 0,
    fontFamily: "RobotoMono_400Regular",
    fontSize: 16,
    height: 52,
    paddingBottom: 0,
    paddingLeft: 14,
    paddingRight: 14,
    paddingTop: 0,
    width: "100%",
  },
  s12: {
    flexDirection: "column",
    flexGrow: 0,
    flexShrink: 0,
    gap: 6,
    width: 96,
  },
  s13: {
    alignItems: "center",
    backgroundColor: "#2563EB",
    borderRadius: 12,
    borderWidth: 0,
    flexDirection: "row",
    flexShrink: 0,
    height: 56,
    justifyContent: "center",
    marginTop: 4,
    width: "100%",
  },
  s14: {
    color: "#fff",
    fontFamily: "Inter_600SemiBold",
    fontSize: 16,
    lineHeight: 16,
    textAlign: "center",
  },
  s15: {
    color: "#5B6B7F",
    fontFamily: "Inter_400Regular",
    fontSize: 12,
    lineHeight: 18,
    textAlign: "center",
  },
  passwordBox: {
    alignItems: "center", backgroundColor: "#FFFFFF", borderColor: "#E2E8F0", borderRadius: 10,
    borderWidth: 1, flexDirection: "row", height: 52, paddingLeft: 14, paddingRight: 14,
  },
  passwordInput: { color: "#0F1B2A", flex: 1, fontFamily: "Inter_400Regular", fontSize: 16, height: "100%", padding: 0 },
  inputError: { borderColor: "#DC2626" },
  errorText: { color: "#DC2626", fontFamily: "Inter_400Regular", fontSize: 11, lineHeight: 15 },
});
